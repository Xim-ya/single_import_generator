import 'package:flutter/material.dart';

class KeepAliveView extends StatelessWidget {
  const KeepAliveView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
